diff --git a/CLAUDE.md b/CLAUDE.md
index 181e279..a3d1cfa 100644
--- a/CLAUDE.md
+++ b/CLAUDE.md
@@ -147,7 +147,7 @@
 
 详见 docs/architecture.md 和项目 plan 文件。
 
-当前阶段：阶段 7 — 真实 MCP stdio / Claude Code 接入验证。
+当前阶段：阶段 7.2 — MCP stdio 客户端补强（缺mcp exit语义/argparse/importorskip/--allow-missing-mcp）。
 
 ## 硬性规则（MCP 层）
 
diff --git a/README.md b/README.md
index 7a129ed..a6af979 100644
--- a/README.md
+++ b/README.md
@@ -88,8 +88,11 @@ Claude Code 配置文档：`docs/claude-code-mcp-setup.md`
 # 验证 MCP server 可启动
 python scripts/check_mcp_server.py
 
-# 真实 MCP stdio client 调用测试
-python scripts/test_mcp_stdio_client.py
+# 推荐：临时库 + demo 数据，不污染当前 data/memory.db
+python scripts/test_mcp_stdio_client.py --seed-demo
+
+# 真实当前库验证，不推荐频繁使用
+python scripts/test_mcp_stdio_client.py --use-current-db
 ```
 
 ## 本地完整验证
diff --git a/docs/claude-code-mcp-setup.md b/docs/claude-code-mcp-setup.md
index bb5aeff..0c1efdc 100644
--- a/docs/claude-code-mcp-setup.md
+++ b/docs/claude-code-mcp-setup.md
@@ -2,11 +2,15 @@
 
 ## 接入前检查
 
-运行以下命令确认 MCP server 可启动：
-
 ```bash
+# 推荐：临时库 + demo 数据，不污染当前 data/memory.db
+python scripts/test_mcp_stdio_client.py --seed-demo
+
+# 如果要用当前真实数据库验证（会写入测试知识，不推荐频繁使用）
+python scripts/test_mcp_stdio_client.py --use-current-db
+
+# 基础检查
 python scripts/check_mcp_server.py
-python scripts/test_mcp_stdio_client.py
 python scripts/dev_check.py
 python scripts/diagnose.py
 ```
diff --git a/scripts/test_mcp_stdio_client.py b/scripts/test_mcp_stdio_client.py
index aea301a..8d5f791 100644
--- a/scripts/test_mcp_stdio_client.py
+++ b/scripts/test_mcp_stdio_client.py
@@ -2,16 +2,18 @@
 真实 MCP stdio client 测试 — 通过 stdio 启动 server 并调用 tools。
 
 使用：
-    python scripts/test_mcp_stdio_client.py
-    exit 0 = 测试通过, exit 1 = 失败
-
-如果本机未安装 mcp 或 client API 不可用，输出清晰提示并 exit 0 跳过。
+    python scripts/test_mcp_stdio_client.py --seed-demo  (推荐)
+    python scripts/test_mcp_stdio_client.py --allow-missing-mcp  (CI/无mcp跳过)
+    python scripts/test_mcp_stdio_client.py --use-current-db  (真实库，谨慎)
 """
 
+import argparse
 import asyncio
 import json
 import os
+import shutil
 import sys
+import tempfile
 from pathlib import Path
 
 _PROJECT_ROOT = Path(__file__).resolve().parent.parent
@@ -31,7 +33,7 @@ def _ok(msg: str):
     print(f"  [OK] {msg}")
 
 
-async def _run_tests():
+async def _run_tests(config_dir: str, db_path: str, log_dir: str, seed_demo: bool):
     global _exit_code
     try:
         from mcp.client.stdio import stdio_client, StdioServerParameters
@@ -41,23 +43,19 @@ async def _run_tests():
         return
 
     python = sys.executable
-    config_dir = os.environ.get("PROJECT_MEMORY_CONFIG_DIR", str(_PROJECT_ROOT / "config"))
-    db_path = os.environ.get("PROJECT_MEMORY_DB_PATH", str(_PROJECT_ROOT / "data" / "memory.db"))
-
     server_params = StdioServerParameters(
         command=python,
         args=["-m", "project_memory_mcp"],
         env={
             "PROJECT_MEMORY_CONFIG_DIR": config_dir,
             "PROJECT_MEMORY_DB_PATH": db_path,
-            "PROJECT_MEMORY_LOG_DIR": os.environ.get("PROJECT_MEMORY_LOG_DIR", str(_PROJECT_ROOT / "logs")),
+            "PROJECT_MEMORY_LOG_DIR": log_dir,
             "PROJECT_MEMORY_LOG_LEVEL": "INFO",
             "PYTHONIOENCODING": "utf-8",
         },
     )
 
     print(f"启动 server: {python} -m project_memory_mcp")
-    print(f"config_dir = {config_dir}")
 
     try:
         async with stdio_client(server_params) as (read, write):
@@ -74,20 +72,19 @@ async def _run_tests():
                 else:
                     _fail(f"期望 >=9 个 tools, 实际 {len(tool_names)}")
 
-                # 2. call list_projects
+                # 2. list_projects
                 r = await session.call_tool("list_projects", {"status_filter": "active"})
                 result = json.loads(r.content[0].text)
                 if result.get("ok"):
                     projects = result.get("data", {}).get("projects", [])
                     _ok(f"list_projects: {len(projects)} 个项目")
-                    print(f"    slugs: {[p.get('slug') for p in projects]}")
                 else:
                     _fail(f"list_projects 失败: {result}")
 
-                # 3. call search_project_context
+                # 3. search_project_context
                 r = await session.call_tool("search_project_context", {
                     "project_id": "biaopai-erp",
-                    "query": "产品",
+                    "query": "产品 材料",
                     "max_results": 5,
                 })
                 result = json.loads(r.content[0].text)
@@ -95,20 +92,24 @@ async def _run_tests():
                     cp = result.get("data", {}).get("context_pack", {})
                     n = len(cp.get("project_context", []))
                     print(f"  search: {n} 条结果 (project_context)")
-                    _ok("search_project_context 成功")
+                    _ok("search_project_context 调用成功")
+                    if seed_demo:
+                        if n > 0:
+                            titles = [x.get("title", "") for x in cp.get("project_context", [])]
+                            _ok(f"demo search 返回结果: {titles}")
+                        else:
+                            _fail("demo search 返回 0 条结果，期望 >= 1")
                 else:
                     code = result.get("error", {}).get("code", "unknown")
-                    print(f"  search 返回: code={code}")
-                    # project_not_found 是可接受的（测试配置可能没有 biaopai-erp）
-                    if code in ("project_not_found",):
-                        _ok("search_project_context 返回稳定错误码")
+                    if code == "project_not_found":
+                        _ok("search 返回 project_not_found（测试配置可能无此项目）")
                     else:
-                        _fail(f"search_project_context 失败: {result}")
+                        _fail(f"search 失败: {result}")
 
-                # 4. call propose_memory (普通内容)
+                # 4. propose_memory (普通)
                 r = await session.call_tool("propose_memory", {
                     "project_id": "biaopai-erp",
-                    "title": "MCP stdio 测试知识",
+                    "title": "[STDIO_TEST] MCP stdio 测试知识",
                     "content": "通过 stdio client 提交的测试知识",
                     "actor": "stdio-test",
                 })
@@ -116,21 +117,18 @@ async def _run_tests():
                 if result.get("ok"):
                     status = result.get("data", {}).get("status")
                     print(f"  propose status={status}")
-                    if status in ("pending_review", "approved", "rejected"):
-                        _ok(f"propose_memory status={status}")
-                    else:
-                        _fail(f"未知状态: {status}")
+                    _ok(f"propose_memory status={status}")
                 else:
                     code = result.get("error", {}).get("code")
                     if code == "project_not_found":
-                        _ok("propose_memory project_not_found（可用测试配置）")
+                        _ok("propose project_not_found")
                     else:
-                        _fail(f"propose_memory 失败: {result}")
+                        _fail(f"propose 失败: {result}")
 
-                # 5. call propose_memory (blocked)
+                # 5. propose_memory (blocked)
                 r = await session.call_tool("propose_memory", {
                     "project_id": "biaopai-erp",
-                    "title": "私钥测试",
+                    "title": "[STDIO_TEST] 私钥测试",
                     "content": "-----BEGIN RSA PRIVATE KEY-----\ntest",
                     "actor": "stdio-test",
                 })
@@ -143,7 +141,7 @@ async def _run_tests():
                 else:
                     code = result.get("error", {}).get("code", "unknown")
                     if code == "project_not_found":
-                        _ok("propose_memory 返回 project_not_found")
+                        _ok("propose blocked 返回 project_not_found")
                     else:
                         _fail(f"blocked propose 失败: {result}")
 
@@ -152,6 +150,68 @@ async def _run_tests():
     except Exception as exc:
         _fail(f"stdio client 异常: {type(exc).__name__}: {exc}")
 
+
+def main():
+    global _exit_code
+    p = argparse.ArgumentParser(description="MCP stdio client 测试")
+    p.add_argument("--seed-demo", action="store_true", help="填充 demo 数据并验证搜索")
+    p.add_argument("--use-current-db", action="store_true", help="使用真实 data/memory.db（会写入）")
+    p.add_argument("--keep-temp", action="store_true", help="保留临时目录")
+    p.add_argument("--allow-missing-mcp", action="store_true", help="没装 mcp 时 skip 而非失败")
+    p.add_argument("--timeout", type=int, default=60, help="总超时秒数")
+    args = p.parse_args()
+
+    # 检查 mcp
+    try:
+        import mcp  # noqa: F401
+    except ImportError:
+        if args.allow_missing_mcp:
+            print("mcp 未安装，按 --allow-missing-mcp 跳过")
+            return 0
+        print("mcp 包未安装，请先 pip install -e '.[dev]' 或 pip install mcp")
+        return 1
+
+    keep_temp = args.keep_temp
+    seed_demo = args.seed_demo
+
+    if args.use_current_db:
+        config_dir = str(_PROJECT_ROOT / "config")
+        db_path = str(_PROJECT_ROOT / "data" / "memory.db")
+        log_dir = str(_PROJECT_ROOT / "logs")
+        print("模式: --use-current-db (会写入真实 data/memory.db)")
+    else:
+        tmp = tempfile.mkdtemp()
+        config_dir = str(Path(tmp) / "config")
+        db_path = str(Path(tmp) / "data" / "memory.db")
+        log_dir = str(Path(tmp) / "logs")
+        Path(config_dir).mkdir(parents=True)
+        src_yml = _PROJECT_ROOT / "config" / "projects.yml"
+        if src_yml.exists():
+            shutil.copy(str(src_yml), str(Path(config_dir) / "projects.yml"))
+        print(f"模式: 临时库 ({tmp})")
+
+    # 初始化数据库 + sync + 可选 seed
+    try:
+        sys.path.insert(0, str(_PROJECT_ROOT / "src"))
+        from project_memory_mcp.app_context import AppContext
+        ctx = AppContext(config_dir=Path(config_dir), db_path=Path(db_path))
+        ctx.sync_projects()
+
+        if seed_demo:
+            from scripts.seed_demo_data import seed
+            seed_result = seed(ctx)
+            print(f"  演示数据: 新增 {seed_result['created']}, 跳过 {seed_result['skipped']}")
+
+        ctx.db.close()
+    except Exception as exc:
+        _fail(f"初始化失败: {exc}")
+        return _exit_code
+
+    try:
+        asyncio.run(_run_tests(config_dir, db_path, log_dir, seed_demo))
+    except KeyboardInterrupt:
+        print("\n用户中断")
+
     print(f"\n{'=' * 50}")
     if _exit_code == 0:
         print("  MCP stdio client 测试全部通过")
@@ -159,12 +219,9 @@ async def _run_tests():
         print("  MCP stdio client 测试存在失败项")
     print(f"{'=' * 50}")
 
+    if not args.use_current_db and not keep_temp and 'tmp' in dir() and Path(tmp).exists():
+        shutil.rmtree(tmp, ignore_errors=True)
 
-def main():
-    try:
-        asyncio.run(_run_tests())
-    except KeyboardInterrupt:
-        print("\n用户中断")
     return _exit_code
 
 
diff --git a/tests/test_mcp_stdio.py b/tests/test_mcp_stdio.py
index 4c4cefe..17fe128 100644
--- a/tests/test_mcp_stdio.py
+++ b/tests/test_mcp_stdio.py
@@ -1,7 +1,5 @@
 """
 MCP stdio 相关测试 — check_mcp_server / test_mcp_stdio_client 脚本可运行。
-
-不直接启动 stdio server（避免卡死）。
 """
 
 import os
@@ -24,6 +22,14 @@ def _run(name: str, env: dict, timeout: int = 30):
     )
 
 
+def _run_with_args(name: str, args: str, env: dict, timeout: int = 60):
+    return subprocess.run(
+        [sys.executable, str(_SCRIPTS_DIR / name)] + args.split(),
+        env=env, cwd=str(_PROJECT_ROOT),
+        capture_output=True, text=True, timeout=timeout,
+    )
+
+
 def _write_config(config_dir: Path):
     config_dir.mkdir(parents=True, exist_ok=True)
     (config_dir / "projects.yml").write_text("""\
@@ -50,56 +56,63 @@ defaults:
 
 def test_check_mcp_server_runs():
     """check_mcp_server.py 可运行且 exit 0。"""
+    pytest.importorskip("mcp")
     tmp = Path(tempfile.mkdtemp())
     try:
         config_dir = tmp / "config"
         db_path = tmp / "data" / "memory.db"
         _write_config(config_dir)
-
         env = os.environ.copy()
         env["PROJECT_MEMORY_CONFIG_DIR"] = str(config_dir)
         env["PROJECT_MEMORY_DB_PATH"] = str(db_path)
-
         r = _run("check_mcp_server.py", env)
-        assert r.returncode == 0, f"exit={r.returncode}\nstdout={r.stdout}\nstderr={r.stderr}"
+        assert r.returncode == 0, f"exit={r.returncode}\n{r.stderr}"
         assert "MCP server 创建成功" in r.stdout
     finally:
         import shutil
         shutil.rmtree(str(tmp), ignore_errors=True)
 
 
-def test_stdio_client_handles_no_mcp(tmp_path, monkeypatch):
-    """没有 mcp 时 test_mcp_stdio_client 不崩溃。"""
-    monkeypatch.setattr(
-        "scripts.test_mcp_stdio_client.asyncio.run",
-        lambda coro: None,
-    )
-    # 不会崩溃即可
-    assert True
+def test_stdio_client_allow_missing_mcp():
+    """--allow-missing-mcp 在 mcp 存在时也应 exit 0。"""
+    pytest.importorskip("mcp")
+    tmp = Path(tempfile.mkdtemp())
+    try:
+        config_dir = tmp / "config"
+        _write_config(config_dir)
+        env = os.environ.copy()
+        env["PROJECT_MEMORY_CONFIG_DIR"] = str(config_dir)
+        env["PROJECT_MEMORY_DB_PATH"] = str(tmp / "data" / "memory.db")
+        r = _run_with_args("test_mcp_stdio_client.py", "--allow-missing-mcp", env, timeout=60)
+        assert r.returncode == 0, f"exit={r.returncode}\n{r.stderr}"
+    finally:
+        import shutil
+        shutil.rmtree(str(tmp), ignore_errors=True)
+
+
+def test_stdio_client_help():
+    """--help 输出包含 --use-current-db 说明。"""
+    r = _run_with_args("test_mcp_stdio_client.py", "--help", os.environ.copy())
+    assert "use-current-db" in r.stdout.replace("-", "").replace("_", "").lower() \
+        or "current" in r.stdout.lower()
 
 
 def test_create_server_registers_9_tools():
     """create_server(ctx) 注册 9 个 tools。"""
     pytest.importorskip("mcp.server.fastmcp")
-
     tmp = Path(tempfile.mkdtemp())
     try:
         config_dir = tmp / "config"
         db_path = tmp / "data" / "memory.db"
         _write_config(config_dir)
-
         os.environ["PROJECT_MEMORY_CONFIG_DIR"] = str(config_dir)
         os.environ["PROJECT_MEMORY_DB_PATH"] = str(db_path)
-
         from project_memory_mcp.app_context import AppContext
         from project_memory_mcp.server import create_server
-
         ctx = AppContext(config_dir=config_dir, db_path=db_path)
         server = create_server(ctx=ctx)
-
         tools = server._tool_manager._tools if hasattr(server, '_tool_manager') else {}
         tool_names = list(tools.keys()) if tools else []
-
         expected = [
             "list_projects", "resolve_project", "get_project_profile",
             "search_project_context", "propose_memory", "list_memories",
@@ -107,7 +120,6 @@ def test_create_server_registers_9_tools():
         ]
         for name in expected:
             assert name in tool_names, f"缺少 tool: {name}"
-
         ctx.db.close()
     finally:
         import shutil
